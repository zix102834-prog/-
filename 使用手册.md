# 视觉复刻师使用手册

## 1. 适用场景

当你想借鉴一张海报的视觉逻辑、但需要换成自己的商品、场景和卖点时使用本 Skill。它适合餐饮海报、产品广告、活动主视觉和带强版式层级的宣传图。

不适合要求逐像素临摹、保留原品牌、原人物或原文案的任务。

## 2. 四类输入的职责

| 输入 | 是否必需 | 控制内容 |
| --- | --- | --- |
| 原图 | 必需 | 风格、氛围、材质、摄影或插画气质、色彩关系 |
| 视觉拆解图 | 必需 | 构图区域、信息层级、阅读动线、文字位置 |
| 使用者素材图 | 可选 | 新主体的外观、细节、形状和身份 |
| 确认提示词 | 必需 | 新商品、环境、动作、卖点、文案和排除项 |

没有素材图也可以工作：提示词会补足主体的数量、形状、材质、颜色与关键细节。

## 3. 标准工作流

开始前，Skill 会选择最小运行模式：

- `analysis-only`：只拆解和制作标注图。
- `migration-planning`：拆解、迁移方案和生图提示词。
- `generate-now`：用户明确要求时，在规划完成后直接生图并复核结果。

### 第一步：给出原图

上传完整海报，并说明你想要「拆解」「迁移方案」还是「直接生成」。例如：

```text
使用视觉复刻师，拆解这张餐饮海报。我要换成一个新产品，素材图暂时没有。
```

### 第二步：确认视觉拆解

Skill 会识别：

- 第一阅读入口，例如左上大标题；
- 第二视觉中心，例如画面中下的大商品；
- 卖点、情绪短句、品质标签和底部收尾的位置；
- 背景、主色、辅助色和高光色的分工；
- 可继承关系与必须排除的来源身份元素。

拆解图优先由 `scripts/create-deconstruction-board.ps1` 确定性生成：原图以 1:1 尺寸放入扩展画布，标注全部位于原图区域外，结果另存为 PNG，并生成 `.integrity.json`。只有源图与嵌入区域的像素 SHA-256 一致时才视为通过。脚本不会覆盖原图；若当前环境无法安全合成，Skill 应交付文字/YAML 拆解，而不是用生成式模型重画原图。

若原图有强标题层级，前缀、属性词或工艺词必须更小，核心感官卖点字必须最大，不能把所有字做成同样大小。

### 第三步：查看迁移方案

「替换方案与提示词专家」会基于拆解 YAML 产出三类候选：

1. 主体：例如从碗装红烧肉迁移到红烧牛肉面、鲜肉生煎或炭烤羊肉串。
2. 环境：例如深色摄影棚、暖铜色烘焙台、深色岩石与余烬暖光。
3. 动作／状态：例如浇淋、拉丝、冒蒸汽、开裂、夹起或现烤出炉。

每项均附独立可复制的提示词，并提供 2–3 个可直接生图的完整组合方向。

它会根据任务选择规划模式：方向开放时用 `explore`；固定部分内容时用 `refine`；主体、场景和动作均已确定时用 `finalize`，只输出一套完整提示词，不再强行发散。

### 第四步：确认生图方向

你只需确认一套组合和文案。建议同时说明：

- 商品名称与核心卖点；
- 是否需要使用你的素材图；
- 画面比例；
- 是否保留某个构图位置或文字区；
- 明确不希望出现的元素。

### 第五步：进入生图

将下列内容送入图像生成工具：

```text
原图：控制风格
视觉拆解图：控制结构
确认提示词：控制新内容
素材图（可选）：控制新主体身份
```

## 4. 子智能体：替换方案与提示词专家

该子智能体不重新分析图片、不生成图片、不替用户做最终选择。它只读取主智能体已经完成的视觉拆解 YAML，以及用户的固定约束和可选素材描述。

它必须交付：

- 3–5 个主体方案；
- 3–5 个环境方案；
- 3–5 个动作或状态方案；
- 2–3 个完整迁移方向；
- 每套方向对应的正向提示词与负面提示词；
- 对应的视觉锚点依据与质量检查。

若当前环境没有多智能体能力，主智能体会按同一份契约在本地完成这一步。

## 5. 常见问题

### 为什么生成图看起来像原海报？

通常是提示词把原图文字、包装、人物、地点或商品身份带了进去。保留的只能是抽象关系，例如「左上主标题 + 中下主体 + 右上卖点」；所有内容必须重新设计。

### 我没有产品素材图怎么办？

直接描述产品。至少交代主体数量、镜头角度、轮廓、材质、主色、关键配料或纹理，以及是否有浇淋、蒸汽或拉丝等状态。

### 标题为什么不够有冲击力？

不要把标题等大排开。将修饰词缩小，让核心感官卖点字成为最大视觉字。例如「炭火香」应突出「香」；「热蜂蜜脆鸡堡」中可以将「脆鸡堡」明显放大。

### 图中出现了来源水印或原文案怎么办？

把它们加入负面提示词，并强调「仅继承结构，不继承来源内容」。若要商业发布，确认所有输入图片和文字均可合法使用。
